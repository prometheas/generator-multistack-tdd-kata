{
  "name": "<%= kata.slug %>-with-mocha",
  "version": "<%= version %>",
  "description": "Mocha-based TDD kata for <%= kata.slug %>",
  "main": "index.js",
  "scripts": {
    "test": "mocha --glob '*.spec.js' --recursive test"
  },
  "author": "<%= name %>",
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {
    "chai": "^4.3.0",
    "mocha": "^9.2.0"
  }
}
